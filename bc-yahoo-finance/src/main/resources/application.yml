server:
  port: 9085

database:
  db-name: "bc-yahoo-finance"
  
spring: 
  datasource:
    driverClassName: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:5432/${database.db-name}"
    username: "postgres"
    password: "admin1234"

spring.jpa:
  show-sql: true
  hibernate.ddl-auto: update #create-drop # update
  # properties: 
